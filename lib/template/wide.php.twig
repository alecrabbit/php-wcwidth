<?php

declare(strict_types=1);

namespace AlecRabbit\WCWidth;

// @codeCoverageIgnoreStart
const WIDE_EASTASIAN = [
{% for version, d in data %}
    '{{ version }}' => [
{% for entry in d %}
        [{{ entry.normalizedStart }}, {{ entry.normalizedEnd }},],     // ; {{ entry.properties }}     # {{ entry.comment }}
{% endfor %}
    ],
{% endfor %}
];
// @codeCoverageIgnoreEnd
